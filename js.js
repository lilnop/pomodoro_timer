let sessionTime=document.querySelector("#session-timer");let breakTime=document.querySelector("#break-timer");let decrement=document.querySelector(".decrement");let increment=document.querySelector(".increment");let bDecrement=document.querySelector(".b-decrement");let bIncrement=document.querySelector(".b-increment");let count=parseInt(document.getElementById("session-timer").textContent);let bCount=parseInt(document.querySelector("#break-timer").textContent);let start=document.querySelector("#start");let reset=document.querySelector("#reset");let timeUp=document.querySelector("#time-up");decrement.addEventListener("click",function(){if(count>1){count=count-1;sessionTime.textContent=count;}});increment.addEventListener("click",function(){count=count+1;sessionTime.textContent=count;});bDecrement.addEventListener("click",function(){if(bCount>1){bCount-=1;breakTime.textContent=bCount;}});bIncrement.addEventListener("click",function(){bCount+=1;breakTime.textContent=bCount;});start.addEventListener("click",function(){start.disabled=true;reset.disabled=true;let counting=setInterval(function timer(){count-=1;if(count%60>=10){sessionTime.textContent=Math.floor(count / 60)+":"+count%60;}
else{sessionTime.textContent=Math.floor(count / 60)+":"+"0"+count%60;}
increment.style.display="none";decrement.style.display="none";bDecrement.style.display="none";bIncrement.style.display="none";if(count===0){clearInterval(counting);timeUp.play();bCount*=60;let bCounter=setInterval(function bTimer(){bCount-=1;reset.disabled=true;if(bCount%60>=10){breakTime.textContent=Math.floor(bCount / 60)+":"+bCount%60;}
else{breakTime.textContent=Math.floor(bCount / 60)+":"+"0"+bCount%60;}
if(bCount===0){clearInterval(bCounter);reset.disabled=false;if(reset.disabled===false){resetT;clearInterval(bCounter);}}},1000);}},1000);count*=60;});let resetT=reset.addEventListener("click",function(){count=25;bCount=5;sessionTime.textContent=count;breakTime.textContent=bCount;start.disabled=false;reset.disabled=true;if(decrement.style.display==="none"){decrement.style.display="block";}
if(increment.style.display==="none"){increment.style.display="block";}
if(bDecrement.style.display==="none"){bDecrement.style.display="block";}
if(bIncrement.style.display==="none"){bIncrement.style.display="block";}});